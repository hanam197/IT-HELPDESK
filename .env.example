# Generate secrets, for example: openssl rand -hex 32
POSTGRES_PASSWORD=replace_with_random_alphanumeric_password
SECRET_KEY=replace_with_a_random_secret_at_least_32_characters
SEED_PASSWORD=replace_with_demo_password_minimum_12_characters
FRONTEND_URL=http://localhost:8080
HTTP_PORT=8080
# Set true when served through HTTPS.
SECURE_COOKIE=false
